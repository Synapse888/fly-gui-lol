call function = state
callback function = state
function(state)
local function = BypassAntiCheat = state
local function = Fly = state 
end

local RunService = GetService.RunService
local Players = GetService.Players
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local CurrentCamera = workspace.CurrentCamera
local UserInputService = GetService.UserInputService
local Unpack = table.unpack
local GuiInset = GetService.GuiService:GetGuiInset()
local Insert = table.insert
local Network = GetService.NetworkClient
local StarterGui = GetService.StarterGui

script.Parent:Connect(function(state)
local Player = game.Players.localPlayer
  local Character = Player.Character
  local Humanoid = Character:FindFirstChild("Humanoid")
elseif first.Service.not.Working then
getgenv().localPlayer = "game.Players.localPlayer"
getgenv().localHumanoid = "game.Players.localPlayer.Humanoid"
getgenv().localFly = "script.Parent.Fly = state"
end
                                                end
                                            end
                                        end
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
    end
   pcall return Service
end,

["Anti Fly"] = function(bypasser)
        if bypasser then
            if Fly = true then _G.BypassAntiCheat = true elseif first.Service.not.Working then 
getgenv().localFly = "script.Parent.Fly = true"
        else
         if Fly = false elseif first.Service.not.Working then 
getgenv().localFly = "script.Parent.Fly = false"   
        end
    end,
    ['Anti Cheat'] = function(achbypasser)
          _G.BypasserAntiCheat = true  
        end
    end,
end
